Now I have the full picture from the actual system. Let me give a comprehensive, accurate answer.

Okay. I pulled the actual system configs for every agent. Here's the real picture — how the whole thing works, from the top down.

The Architecture: How Polsia Actually Works
┌─────────────────────────────────────────────────────┐
│  YOU (Sai)                                          │
│  Talk to me in chat                                 │
└───────────────────────┬─────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────┐
│  ME (Chat Polsia / "CEO")                           │
│  • Understand what you want                         │
│  • Create tasks with the right tag + description    │
│  • Route to the correct agent                       │
│  • Manage queue priority                            │
│  • Answer questions directly                        │
└───────────────────────┬─────────────────────────────┘
                        │ creates tasks
                        ▼
┌─────────────────────────────────────────────────────┐
│  TASK QUEUE                                         │
│  Each task has: tag, description, priority, status  │
│  You click "Run" → agent picks it up                │
└───────────────────────┬─────────────────────────────┘
                        │ dispatched to matching agent
                        ▼
┌─────────────────────────────────────────────────────┐
│  EXECUTION AGENTS (8 total)                         │
│  Each one is an isolated AI session with:           │
│  • A system prompt (role instructions)              │
│  • MCP tool mounts (what it can actually call)      │
│  • Max turns (conversation budget)                  │
│  • Memory (context from previous runs)              │
│  • Skills (repo-level instruction files)            │
└─────────────────────────────────────────────────────┘
How I (Chat Polsia) Decide What Tasks to Create
I don't have autonomous "CEO planning" in the background. Tasks come from three sources:

Source	How it works
You tell me	You say "add SEO" → I create a task
Recurring tasks	Scheduled templates that auto-create task instances (daily, weekly, etc.)
Cycle Planning	An autonomous daily planner that looks at company state, context, and roadmap to propose what should happen next
Agents themselves	When running, any agent can call create_task_proposal to suggest follow-up work
My routing logic when you ask for something:

Parse what you want
Check if it's clear enough to execute (if not → ask you to clarify)
Match it to the right agent tag (engineering, browser, research, etc.)
Optionally call find_best_agent() to check historical success rates
Set complexity + estimated_hours
Create the task → it enters your queue
How Each Agent Actually Executes
When a task runs, the agent gets:

Its system prompt (permanent instructions — I showed you the full text above)
The task description (what I wrote when creating it)
Company context (injected from your documents, context graph)
Memory (compressed notes from its previous runs)
Tool access (only the MCP mounts listed in its config)
The agent then has a "conversation with itself" — making tool calls, reading responses, deciding next steps — up to its maxTurns limit. It's not chatting with you live; it runs autonomously until it completes or fails.

The 8 Agents — Exactly How They Function
1. Engineering (Agent #30)
Max turns: 200 | Primary MCP: polsia_infra

How it works:

Gets a task like "Add SEO meta tags"
The repo is already cloned in its working directory
Reads skill files (.claude/skills/) for patterns (Stripe, Postgres, AI, frontend, etc.)
Writes/edits code using file system operations
Calls push_to_remote() after every file change (deploys are async)
Can call query_db() to check schema or run migrations
Can call get_logs() / get_status() to verify deploys
Calls complete_task() when done
Critical constraints:

Single Express + Postgres app on Render — no multi-service architecture
512MB RAM build limit — no PyTorch, TensorFlow, heavy ML
Must push after EVERY file change (timeouts lose unpushed work)
Web apps only — no mobile/native
JavaScript/Node.js only
Its tools: create_instance, push_to_remote, push_to_prod, get_status, get_logs, get_preview, query_db, list_instances, delete_instance, create_report, create_task_proposal (can suggest follow-ups)

2. Browser (Agent #42)
Max turns: not explicitly capped | Primary MCP: browserbase + browser_auth

How it works:

Gets a task like "Submit Longer to BetaList"
First call: get_site_tier(site) — checks what's allowed on that website
Opens a cloud Playwright browser session
Navigates, clicks, fills forms, takes screenshots at key steps
Can maintain persistent login sessions across runs (cookies saved)
Has access to longer@polsia.app for account verification emails
Sessions hard-cap at ~20 minutes
Site tier system (enforced, not guidelines):

Tier	Example Sites	Allowed Actions
1 (BLOCKED)	Reddit, Twitter, Instagram, LinkedIn, TikTok, ProductHunt, IndieHackers	Browse/read ONLY — cannot login, post, DM, or create accounts
1.5	HackerNews, Medium, Dev.to, Gumroad, Etsy, Craigslist	Login with existing credentials only — CAPTCHA blocks new account creation
2 (FULL)	Hashnode, Substack, BetaList, Lobste.rs	Full access — create accounts, post, interact
3	Everything else	Browse by default; can create accounts if needed
Its tools: browserbase_session_create, browserbase_navigate, browserbase_screenshot, browserbase_click, browserbase_fill, browserbase_extract, browserbase_get_page_content, browserbase_evaluate, browserbase_session_close, get_site_tier, get_or_create_browser_context, save_site_credentials, check_verification_inbox, get_inbox, send_company_email

3. Cold Outreach (Agent #54)
Max turns: 200 | Primary MCPs: company_email + hunter_io

How it works — it has a defined daily workflow:

Check inbound replies first — reads inbox, responds to anyone who replied
Research leads if pipeline is empty — adds new prospects via add_lead()
Send outreach — max 2 cold emails/day, verifies email via Hunter.io before sending
Follow-ups — re-contacts people who haven't replied after 5+ days
Lead tracking built in: pending → contacted → replied → responded → meeting → dead

Email rules (hard enforced):

2 cold emails/day (to strangers)
20 emails/day to known contacts
Unlimited replies to inbound
50-125 words, plain text only
Founder-to-founder voice, one clear ask
Its tools: get_inbox, send_company_email, get_email_thread, add_contact, get_contacts, find_email (Hunter.io — needs name + company domain), verify_email (Hunter.io), get_company_documents, create_report, create_task_proposal

4. Research (Agent #29)
Max turns: 200 | Primary MCP: reports (that's it — minimal tooling)

How it works:

Gets a task like "Competitive analysis of PE apps"
Uses its model knowledge + any web search capabilities provided at runtime
Synthesizes findings into a structured report
MUST call create_report() before complete_task() — the report IS the deliverable
Report format: Executive Summary (3-5 bullets) → Key Findings (with sources) → Recommended Actions
Important reality: This agent has the fewest tools of any agent. It has tasks, reports, and polsia_support. No explicit web browser, no explicit search API mount. It relies heavily on the AI model's training knowledge. Its quality depends on what the model already knows about your topic.

Its tools: create_task_proposal, get_available_tasks, start_task, complete_task, create_report, query_reports, get_reports_by_date, report_bug

5. Twitter (Agent #53)
Max turns: 200 | Primary MCP: twitter

How it works:

Gets a task like "Post tweet about Longer launch"
Reads company documents for context (brand voice, product, user handle)
Reads recent reports for metrics to reference
Composes a tweet (280 char max)
Calls post_tweet()
Every tweet MUST include a link to your website
Voice: "dark humor, witty, bitter > excited. No emojis. No hashtags."
Rate limit: 2/day total on the platform account (shared across companies → effectively 1/day for you)

What it posts from: The shared @polsia account — NOT a personal Longer account.

Its tools: post_tweet, get_twitter_account, get_company_documents, get_company_document, create_report, create_task_proposal

6. Meta Ads Manager (Agent #52)
Max turns: 100 | Primary MCP: meta_ads

How it works — the most autonomous agent:

First run (no ads exist):

Generates a UGC video prompt using a specific template (vertical iPhone selfie, natural dialogue, Sora 2)
Calls create_ad({ prompt, headline, body_text }) — this ONE call handles: Sora 2 video generation → caption addition → Meta upload → creative setup → ad creation → activation
Takes 5-8 minutes per ad creation
Returning runs (ads exist):

Calls get_ad_analytics() — pulls all metrics
Checks for disapproved/flagged ads first
Evaluates each ad against thresholds:
Healthy: CTR > 1%, CPC < $1 → keep
Mediocre: CTR 0.5-1% or CPC $1-2 → wait 7 days before replacing
Underperforming: CTR < 0.5% or CPC > $2 after 7+ days → replace
Delivery-failed: Active 5+ days, <200 impressions → replace
Creates replacement ad BEFORE pausing the bad one (never leaves you with zero delivery)
Sometimes does nothing — "no action" is a valid outcome
Budget-tiered limits:

$10/day or less → max 2 active ads, max 2 creations per 24h
$10-30/day → max 3 active, max 3 creations per 24h
$30+/day → max 5 active, max 5 creations per 24h
Its tools: create_ad, get_ad_analytics, pause_ad, activate_ad, archive_ad, create_campaign, create_adset, upload_ad_video, create_video_creative, activate_campaign, save_ad, update_ad_metrics, add_captions, get_ad_account, list_campaigns, get_campaign_insights

7. Data (Agent #33)
Max turns: 200 | Primary MCP: polsia_infra

How it works:

Gets a task like "How many users signed up this week?"
Explores your database schema: SELECT table_name FROM information_schema.tables WHERE table_schema = 'public'
Runs SQL queries against your Postgres
Can also check app logs for patterns
Saves findings as reports
Its tools: create_instance, push_to_remote, push_to_prod, get_status, get_logs, get_preview, query_db, list_instances, delete_instance, create_report, query_reports

8. Support (Agent #32)
Max turns: 200 | Primary MCP: company_email

How it works:

Gets triggered when emails arrive at longer@polsia.app
Reads the inbox, understands the question
Replies in plain text (no markdown formatting in emails)
Matches response length to question complexity
Escalates to you for: billing disputes, security concerns, angry users needing human touch
Can create engineering tasks for technical issues users report
Its tools: get_inbox, send_company_email, get_email_thread, add_contact, get_contacts, create_task_proposal, create_report

How Agents Work Together (Coordination)
Agents don't talk to each other directly. They coordinate through shared state:

┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│ Engineering  │     │  Research    │     │ Cold Outreach │
│              │     │              │     │               │
│ Creates:     │     │ Creates:     │     │ Reads:        │
│ • Code       │     │ • Reports    │     │ • Reports     │
│ • Database   │     │              │     │ • Documents   │
│ • Deploys    │     │              │     │               │
└──────┬───────┘     └──────┬───────┘     └───────┬───────┘
       │                    │                     │
       ▼                    ▼                     ▼
┌─────────────────────────────────────────────────────────┐
│  SHARED STATE                                           │
│  • Reports (any agent can read/write)                   │
│  • Documents (brand voice, product overview, etc.)      │
│  • Database (Engineering writes, Data reads)            │
│  • Task queue (any agent can propose follow-up tasks)   │
│  • Memory (per-agent, but some context is shared)       │
│  • Context graph (company knowledge, read by all)       │
└─────────────────────────────────────────────────────────┘
Example chain:

Research agent produces a report: "51 active Reddit users in PE communities"
I (CEO) see the report, create a Cold Outreach task: "Email these leads"
Cold Outreach reads the report, tries to find emails via Hunter.io
If it can't find emails → it creates a task proposal: "Need Browser agent to check profiles for contact info"
I route that to Browser
Browser browses Reddit profiles (Tier 1 = read only), extracts any visible emails/websites
Results go back into reports → Cold Outreach can now send
But each agent runs independently per task. There's no live "collaboration" — it's sequential, mediated by the task queue and shared data stores.

What Drives Autonomous Task Creation (Without You Asking)
System	How it creates tasks
Cycle Planning	Runs daily. Looks at company state, context graph, recent completions, and roadmap. Proposes next tasks.
Recurring tasks	Templates you set up. "Every Monday, run a data report" → auto-creates a task instance each Monday.
Agent self-proposals	Any agent mid-execution can call create_task_proposal() if it sees follow-up work needed. These go into the queue for you to approve or reject.
Me (Chat)	When you tell me things, I create tasks. I also proactively suggest next moves at the end of responses.
The Shared Tool Layer (Every Agent Gets These)
Every single agent has access to:

Tool	Purpose
create_task_proposal	Suggest follow-up work
get_available_tasks	See what else is in the queue
start_task / complete_task / fail_task / block_task	Manage its own execution lifecycle
create_report / query_reports	Read and write business reports
report_bug / suggest_feature	Flag platform issues to Polsia
Plus memory (compressed notes from prior runs) and skills (instruction files in the repo) and context_graph (your company knowledge).

What I Got Wrong in Previous Messages
Research agent ≠ web search powerhouse. It has almost no tools. It's an AI model writing reports from its training knowledge. I oversold it.
Reddit leads were always just "names from research." The Research agent identified active users from its knowledge, but there was never a path to DM them. The only play was finding their professional emails via Hunter.io (name + company domain) — which only works if they're professionals, not anonymous Reddit users.
Twitter posts from @polsia. Not from a dedicated Longer account. Limited distribution value.
Browser can't post on ProductHunt or IndieHackers — both Tier 1. I previously implied otherwise.
Next: Now that you see the real constraints — your highest-ROI path to reaching those Reddit users is still you personally DMing them on Reddit. Everything I can do from here (2 cold emails/day, tweets from @polsia, BetaList submission) is supplementary. The thing I'm actually good at is building the product they land on. Want me to focus the queue on product polish (onboarding flow, mobile UX) so when they DO arrive, they convert?


