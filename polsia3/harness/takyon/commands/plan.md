---
description: Ask the CEO to update the operating plan and workspace notes
requires-business: true
priority-band: ceo
allowed-tools: [read, workspace, memory, budget, queue]
---

Plan the next operating move for `$BUSINESS`.

Operator arguments:

`$ARGUMENTS`

Use the workspace map and evidence feed. Update or propose updates to the smallest useful set of business files, especially:

- `ceo/brief.md`
- `goals/`
- `product/`
- `outreach/`
- `campaigns/`
- `memory/`

Do not invent completion. Request deterministic jobs only when the needed capability is available and the budget/control state allows it.
