# Polsia3 Skill References

These files are archived source material copied from the old `polsia3/skills` tree before the legacy Next app was removed.

They are not active skills by themselves. The authoritative Takyon skills live in:

```text
plugins/takyon/skills/
```

When useful, copy or merge ideas from these references into an active Hermes Takyon skill instead of reintroducing the old `polsia3` app.
