# Ephemeral prebuild output + local state. The scaffold is managed (CNG); ios/ and android/ are
# regenerated by `expo prebuild` at build time and must never be committed.
node_modules/
ios/
android/
.expo/
dist/
web-build/
*.log
.DS_Store
