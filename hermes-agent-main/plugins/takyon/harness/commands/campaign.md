---
description: Create or continue the canonical distribution campaign workspace
requires-business: true
priority-band: p2_growth
allowed-tools: [read, workspace, memory, budget, distribution, conversations]
---

Create or continue the canonical distribution campaign for `business:$BUSINESS`.

Operator arguments:

`$ARGUMENTS`

Campaign work must stay under `distribution/campaign/`. Keep the brief, plan, research notes, creative, outreach assets, worklog, and learnings inside that visible campaign workspace. Receipts are hidden audit/debug state, not campaign deliverables.

Before any outward distribution, inspect unresolved inbound conversation state. If people have replied and need attention, handle or escalate that before new posting/outreach.

Use `takyon:market-research` when the campaign still lacks evidence. Use `business_publish_outreach` for outward publish intents, `ugc-video-ad` for copied UGC video ad work, and `takyon-conversation-followup` when replies are too large or noisy to inspect directly. External spend, posting, vendor mutation, and launches require explicit credentials, budget, controls, and audit receipts.
