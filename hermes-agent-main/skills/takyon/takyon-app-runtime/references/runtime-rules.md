# Runtime Rules

- Shared runtime owns auth, sessions, entitlements, checkout, subscriptions, revenue, and usage budgets.
- Product UI may not claim these are live unless the runtime rails are actually wired.
- Missing credentials are blockers, not permission to simulate.
