# Takyon Control Plane — Supabase Plan

Working plan for the next Takyon backend rails. Target source of truth: **Supabase Postgres** (+ Supabase Storage for the filesystem). This replaces the SQLite-first framing.

## Ground Truth (verified by inspection, 2026-05-30)

- The canonical trunk is `takyon/` → `hermes-agent-main` (`takyon-agent 0.14.0`). Its control/business state today is **local SQLite** at `.takyon/state.sqlite3` (`core.py` connects to it; every E2E run dir holds a `state.sqlite3`).
- **The target Supabase is NOT greenfield.** `DATABASE_URL`/`MIGRATION_DATABASE_URL` in `secrets/.env` point at a project that still hosts the **populated `polsia2` ("Argon Clone") control schema** — ~143 user tables, identity centered on `public.profiles` (there is no `users` table), with **live data**: `public.businesses` (~32 rows; `owner_profile_id`, `slug`, `status`, `mode`, `last/next_ceo_wake_at`), `public.billing_accounts` (~15 rows; Stripe-subscription shape — `business_id`, `stripe_customer_id`, `stripe_subscription_id`, `subscription_status`, `current_period_end`), `agent_runs` (~70 rows), plus `business_memberships`, `profile_ai_wallets`, `project_ai_proxy_keys`, `creator_payout_accounts`, `generated_app_users`, `idempotency_keys`. There is **no `takyon` schema**. polsia2's *code* is discarded; its *schema + data on this database are live* and must be treated as a live dependency, not "prior art."
- **Collisions are real, not hypothetical.** The plan's `businesses` and `billing_accounts` names ALREADY EXIST and are populated, under a different shape (profile-based; Stripe-subscription based) — a greenfield `create table public.<name>` would silently shadow them. Confirmed FREE (no `public`/`takyon` object) by the same survey: `users`, `user_api_keys`, `billing_entries`, `custody_accounts`, `custody_entries`. The top-level identity spine (`users`, `user_api_keys`, `owner_user_id`) genuinely does not exist yet.
- **There IS data to reconcile** (`businesses`, `billing_accounts`, `profiles`): the earlier "no real data to migrate / trivial backfill" framing was **wrong and is retracted**. Choosing replace-vs-extend-vs-isolate for each overlapping table is the gating work for Phases 1–2 and MUST precede any DDL (see **Build Discipline** below). Never apply migrations to this shared, populated database without explicit operator confirmation.
- **Decision (2026-05-30, operator): REPLACE polsia2's `public` control tables.** Takyon owns `public`. The Phase 1–2 migrations deliberately **drop/replace** polsia2's overlapping objects (`businesses`, `billing_accounts`, and whatever FK-depends on them) in dependency order, then install the takyon shape; the FREE names (`users`, `user_api_keys`, `billing_entries`, `custody_accounts`, `custody_entries`) create cleanly. polsia2's live rows are treated as **disposable**. **Guardrail (robustness #1):** design and verify the replace on **LOCAL Postgres first**; the live Supabase apply is a *separate* step requiring (a) a fresh Supabase backup/snapshot and (b) an explicit operator go-ahead — `drop` is irreversible. Migrations stay idempotent/re-runnable.
- **Cutover mechanics (built + locally verified, 2026-05-30).** The REPLACE is implemented as two pieces, not a blind drop: **(1) fail-loud REPLACE guards** at the top of `0001_identity_spine.sql` and `0002_ledgers.sql` — if a non-takyon `businesses`/`billing_accounts` is present (detected by absence of `owner_user_id` / `allowance_included_cents`) the migration RAISES and names the teardown, instead of `create table if not exists` silently binding to polsia2's incompatible shape; **(2) a separate gated teardown** `plugins/takyon/db/retire_polsia2_public.sql` that lives OUTSIDE `db/migrations/` (so the test sweep and any forward-migrate never run it), idempotently drops `billing_accounts` then `businesses` with `cascade` (clearing dependents' FK constraints, not their tables), and — crucially — **refuses to drop a table takyon already owns**, so a re-run can never nuke takyon data. **Live cutover order** (only after backup + explicit go-ahead): `retire_polsia2_public.sql` → `0001` → `0002` → …. Verified on local PG by `tests/plugins/test_takyon_retire_polsia2_pg.py` (guard fires on a simulated polsia2 shadow; retire→migrate yields the takyon shape with resolved FKs; teardown is a no-op on a clean DB; re-run preserves takyon rows). **✅ LIVE APPLY DONE (2026-05-31):** with operator go-ahead and the Supabase snapshot explicitly waived (a local insurance CSV of the two dropped tables was taken instead), `retire_polsia2_public.sql` → `0001`…`0010` ran against the live DB (PG 17.6, ref `ddftvmjpfghfrdxhavvp`). Verified: polsia2's `businesses`/`billing_accounts` retired (cascade cleared 87 FK constraints, dependents' rows intact); **23** takyon tables installed, all empty + takyon-shaped, `dispatch_due_wakes()` present; all 10 fail-loud guards passed with no exception → no silent polsia2 shadow; public base tables 105→125. See IMPLEMENTATION_LOG "Runtime Cutover step 1". **Still gated/outstanding:** full retirement of the *rest* of polsia2's `public` (profiles, agent_runs, the now-orphaned `business_id` dependents) still needs a verified table inventory + a Supabase role/grant review (the live read is now authorized; the teardown deliberately retired ONLY the two takyon-colliding roots), AND the runtime serving-flip (Cutover step 5 / item 4) is unstarted — the `./takyon` shell + dashboard still serve from SQLite.
- Supabase project already provisioned (`DATABASE_URL` in `secrets/.env`); the runtime currently only passes it *through* to generated apps.

## Target Architecture

```
Takyon user ──(one opaque API key)──► Control API ─┐
Sub-user ─────(magic-link session)──► App Runtime ─┤
Generated app ─(project gateway key)► AI Gateway ──┤
                                                    ▼
                                        Internal Resolver  (the opaque control plane)
                                  identity → ownership → policy → wallet → shared provider keys → worker routing
                                                    ▼
                              Supabase Postgres (SOURCE OF TRUTH)  +  Supabase Storage (artifacts/filesystem)
                                                    ▼
                              Job queue (FOR UPDATE SKIP LOCKED) → workers (local now, scale out horizontally later)
```

Postgres is the memory. The queue is the control plane. The runtime is a **stateless, replaceable worker** — not the source of truth.

## Robustness Contract (non-negotiable invariants)

1. **All money is integer minor units (cents, `bigint`).** Never floats.
2. **Two ledgers, never merged:** (A) user→platform billing, (B) sub-user→user custody.
3. **Included allowance is NOT money** (capped, never shown as dollars). **Top-ups ARE exact money** (debited exactly, shown as money).
4. **Every state-changing operation is idempotent** (unique idempotency key); replays produce one effect.
5. **One opaque API key per user** is the entire boundary. Platform mints + rotates; user never generates it; user cannot introspect internals.
6. **Almost nothing is per-user.** Shared platform keys for inference, ONE shared X/Meta posting account, ONE shared platform Stripe for collecting sub-user payments. The only per-user external linkage is **optional, deferred Stripe Connect payout**.
7. **Zero-friction onboarding.** A user operates immediately with zero account connections; never gate onboarding on connecting anything.
8. **No silent fallback, no degraded "completed."** Work is `completed` only when it truly happened; otherwise `blocked` (missing config/budget/credential/approval) or `failed` (code/vendor error) — with a reason.
9. **Every external side effect writes a receipt** (vendor, request id, status, object id, raw error).
10. **Generated apps never hold provider keys or DB credentials** — only a project-scoped gateway key.
11. **RLS on every user-scoped table** as defense-in-depth; the runtime is the single trusted writer (service-role).

## The Supabase Schema

Extensions: `pgcrypto` (`gen_random_uuid()`), `citext` (case-insensitive email). IDs are `uuid`. Timestamps `timestamptz default now()`.

### Identity & ownership
- `users(id, auth0_sub citext unique, email citext, status, created_at, stripe_connect_account_id null, stripe_connect_status default 'none', payout_currency default 'usd')` — `auth0_sub` is the stable join key; `email` is a mutable attribute.
- `user_api_keys(id, user_id→users, key_hash bytea unique, prefix text, created_at, revoked_at null, last_used_at null)`
  - **`unique(user_id) where revoked_at is null`** → exactly one active key per user.
  - Verify: hash the presented secret, look up by hash, require `revoked_at IS NULL`, constant-time.
- `businesses` (port existing; slug stays the runtime handle) **+ `owner_user_id→users NOT NULL`**.

### Billing ledger — flow A (user → platform)
- `billing_accounts(user_id pk→users, allowance_included_cents, allowance_used_cents, allowance_period_start, allowance_resets_at, topup_balance_cents default 0)`
- `billing_entries(id, user_id→users, business_slug null, bucket enum('allowance','topup'), kind enum('grant','reserve','settle','refund','topup','debit'), amount_cents, balance_after_cents, idempotency_key unique, job_id null, created_at, metadata jsonb)` — append-only.
  - Reserved = `Σreserve − Σsettle − Σrefund`. Spend **allowance bucket first, then topup**.

### Custody ledger — flow B (sub-users → user, held by platform)
- `custody_accounts(user_id pk→users, owed_balance_cents default 0, paid_out_cents default 0, currency default 'usd')`
- `custody_entries(id, user_id→users, business_slug, kind enum('accrual','app_fee','payout','refund','adjustment'), gross_cents, fee_cents, net_cents, stripe_ref, idempotency_key unique, created_at)`
  - On sub-user payment: `fee = gross * STRIPE_CONNECT_APPLICATION_FEE_BPS/10000`; accrue `net = gross − fee` to `owed_balance`. **Accrued from day one, independent of whether Connect exists.** Payout drains `owed_balance`. Never netted against billing.

### Providers, policy, gateway
- `provider_accounts(id, kind enum('anthropic','openai','fal','tavily','x','meta','stripe_platform'), scope enum('platform'), encrypted_blob bytea null, metadata jsonb)` — all platform-scoped, encrypted with `APP_ENCRYPTION_KEY`. No per-user posting/inference creds.
- `app_execution_policies(business_slug pk, preferred_model_tier, max_runtime_seconds, max_output_bytes, allow_worker_escalation bool, allow_expensive_branches bool, quality_mode, retry_depth, monthly_app_budget_cents)`
- `app_gateway_keys(id, business_slug→businesses, key_hash unique, prefix, revoked_at null, created_at)` — what generated apps receive.

### Jobs, deployments, sub-user tier, idempotency
- `jobs(id, business_slug→businesses, kind, status enum('queued','running','completed','blocked','failed','cancelled'), idempotency_key unique, payload jsonb, result jsonb null, error jsonb null, reserved_billing_entry_id null, attempts int default 0, max_attempts, locked_by null, locked_at null, created_at, updated_at)`
- `wake_schedules(business_slug pk→businesses, kind default 'ceo_wake', enabled bool default true, interval_seconds int not null, next_run_at timestamptz not null, last_enqueued_at null, payload jsonb, created_at, updated_at)` — **the schedule lives here, not in `.takyon/cron/jobs.json`.** Replaces the local file-based cron entirely; the file ticker (`gateway/run.py::_start_cron_ticker`, `cron/scheduler.py`) is retired with the SQLite path (Phase 8). One row per recurring CEO wake; `next_run_at` is advanced by the dispatcher, never by a local process.
- `app_deployments(id, business_slug→businesses, artifact_ref (Storage path), version, url null, status, health_checked_at null)` — `url` set only after a real health check passes.
- Port the sub-user tier as-is, scoped per `business_slug`: `app_users, app_sessions, app_magic_links, app_entitlements, app_plan_policies, app_budgets`.
- `idempotency_keys(key pk, scope, request_hash, response jsonb, created_at)` — API-level idempotency (checkout, top-up, job submit, webhooks).

## Authentication Boundaries

Every entry point resolves to exactly one identity row before any privileged work. Two human/credential tiers, kept hard-separate (per CLAUDE.md: never conflate Takyon users with sub-users).

**Takyon user (top tier) — two credentials, one `users` row:**
- **Human login = Auth0** (already wired in the canonical runtime: `takyon_cli/web_server.py`, `AUTH0_DOMAIN=login.fourmanifold.com`, audience `https://argon.alpha/api`). The runtime validates the Auth0 token, reads the stable OIDC `sub`, and maps it to `users.auth0_sub`. The system already models the operator as an Auth0 subject — the existing `ARGON_LOCAL_AUTH_SUBJECT/EMAIL` bypass is the local-dev stand-in (must be **off in production**). So this formalizes an existing subject, not a net-new IdP.
- **First login = JIT provisioning** (this IS zero-friction onboarding): no `users` row for that `sub` → create `users` + `billing_account` + `custody_account` + mint the first `user_api_key`, in one transaction, with zero account connections required.
- **Programmatic calls = the opaque API key** (`tk_…`), resolved via `user_api_keys.key_hash`. Minted/rotated **only by an Auth0-authenticated dashboard session** — the user never self-serves key creation, and an anonymous caller can never mint one.

**Sub-user (product tier) — already built, reuse as-is:** magic link → hashed session (`app_magic_links` → `app_sessions`; tokens stored as `token_hash` with `revoked_at`/`expires_at`), scoped per `business_slug` → `app_users`. A sub-user is **never** an Auth0 identity and never touches the top tier.

**Machine / service boundaries:**
- Generated app → AI Gateway: **project gateway key** (`app_gateway_keys`), never provider/DB creds.
- Runtime → Postgres: Supabase **service-role** / `DATABASE_URL` — the single trusted writer (bypasses RLS). Never held in a browser.
- Stripe webhooks: **signature** (`STRIPE_WEBHOOK_SECRET`), idempotent on event id.
- Cron dispatch: **`CRON_SECRET`** bearer.
- Shared provider OAuth (X/Meta): platform-level `X_CLIENT_SECRET` / `META_APP_SECRET` → shared accounts, never per-user.

## The API Plan

### Control API — the opaque Takyon-user boundary
Auth: `Authorization: Bearer tk_<prefix>_<secret>` → hash secret → resolve `user_api_keys` → require active → resolve user. Rate-limited **per `user_id`** (Postgres fixed-window now; Upstash Redis at scale). Errors never leak internals (`402 insufficient_balance`, `429 rate_limited`, `403 not_owner`).
- `GET /v1/me` → user, plan, **topup balance as money**, allowance as opaque "included usage remaining" (never dollars).
- `GET /v1/businesses` / `GET /v1/businesses/{slug}` → owned businesses + read-only policy/usage view.
- `GET /v1/usage` → metered usage per business + account.
- `POST /v1/topups` → Stripe Checkout for an exact top-up; webhook credits `topup_balance` idempotently.
- `GET /v1/payout` → custody `owed_balance`. `POST /v1/payout/connect` → optional Connect onboarding. `POST /v1/payout/withdraw` → payout from custody (only if connected).
- **Key rotation is NOT on this API** (the user doesn't own the key). The dashboard/operator surface (Auth0 session) mints a new key + revokes the old **atomically**.
- Opaque by construction: provider keys, control-plane internals, workers, other tenants, raw allowance cost — none are reachable.

### App Runtime API — the sub-user boundary (per business, shared backend)
Owned by `takyon-app-runtime`. Sub-users hit this first (Vercel). Magic-link → session.
- `/auth, /session, /account, /usage, /checkout, /generate, /jobs/submit, /jobs/status`.
- Enforces entitlements + plan policy, records usage against the business `app_budget`, escalates heavy work to `jobs`.
- Sub-user payments collected on the **shared platform Stripe** → accrue to the **owner's custody ledger** minus app fee. Sub-users never see Takyon internals or the owner's account.

### Internal AI Gateway
- `/internal/ai-gateway/messages` — generated apps + app runtime call with the **project gateway key**. Gateway resolves business → policy → **reserves billing** → calls the shared provider key → **settles**. Generated apps never hold provider keys.

## Billing Engine (reserve-then-settle)

Before expensive work: `reserve(estimate)` on the billing account (allowance bucket first, then topup) inside one transaction with a unique idempotency key. Run job. On success: `settle(actual)` + release `(reserved−actual)`. On failure: `refund(reserved)` → state `blocked`/`failed`. If neither bucket covers the estimate → block with exact reason (`402`). A **reconciliation job** continuously asserts `Σentries == cached balances`, `reserved ≥ 0`, and alerts on drift.

## Worker Plane

`jobs` queue with `SELECT … FOR UPDATE SKIP LOCKED` pickup → one job, one worker. Idempotent execution (idempotency key), at-least-once. Result persisted atomically; partial = `blocked`/`failed`, never `completed`. Retries re-check budget (exhausted → `blocked`, not infinite retry). The runtime worker drains the queue under one provider-neutral job contract — the VPS now, scaled out to N stateless workers later. There is **no external job runner**: heavy/build jobs run on the same contract, just on a worker with more headroom. Every side effect writes a receipt.

### Scheduled CEO wakes (cron) — Postgres-native, no local ticker

Wakes are not a separate mechanism: they are due-rows enqueued into the same `jobs` queue. Three pieces, all in the source of truth — no systemd timer, no `jobs.json`, no `.tick.lock`:

1. **Schedule** — rows in `wake_schedules` (above). The table is the truth; nothing watches a local file.
2. **Dispatch (enqueue-when-due)** — runs every minute, gated by `CRON_SECRET` (Auth Boundaries). Preferred home: Supabase **`pg_cron`** calling an in-DB function, so there is no external process to keep alive; equivalently a single `CRON_SECRET`-bearer endpoint hit on an interval runs the same SQL. Enqueue is **exactly-once** via the `jobs.idempotency_key` unique constraint keyed on the fired window:

   ```sql
   -- dispatch_due_wakes(): enqueue, then advance, atomically
   with due as (
     select business_slug, kind, payload, next_run_at, interval_seconds
     from wake_schedules
     where enabled and next_run_at <= now()
     for update skip locked
   ),
   enq as (
     insert into jobs (business_slug, kind, idempotency_key, payload, status)
     select business_slug, kind,
            'wake:'||business_slug||':'||to_char(next_run_at,'YYYYMMDDHH24MI'),
            coalesce(payload,'{}'::jsonb), 'queued'
     from due
     on conflict (idempotency_key) do nothing      -- replays/overlap → one job
     returning business_slug
   )
   update wake_schedules w
   set next_run_at = greatest(now(), w.next_run_at) + (w.interval_seconds || ' seconds')::interval,
       last_enqueued_at = now(), updated_at = now()
   from due where w.business_slug = due.business_slug;
   ```

   Catch-up is bounded by `greatest(now(), next_run_at)` (a host that was down does not fire N backlogged wakes — one enqueue, schedule realigned to now). A missed minute self-heals on the next dispatch because `next_run_at <= now()` stays true until enqueued.
3. **Drain** — the same worker pulls `kind='ceo_wake'` jobs via `FOR UPDATE SKIP LOCKED`, runs the CEO wake turn, persists `result` atomically (`completed` only if it truly ran, else `blocked`/`failed` with a reason), writes a receipt, re-checks budget on retry. The 3-minute hard interrupt and per-business scope isolation from the old cron path move onto the worker as job-execution invariants.

Why this replaces the old ticker cleanly: in SQLite, an external loop was required because nothing watched the file; in Postgres the schedule, queue, idempotency, locking, **and** dispatch (pg_cron) all live in the source of truth, so the runtime stays a stateless, replaceable worker. (Temporal/Inngest remain an optional later upgrade for durable wake loops, not a Phase-6 requirement.)

## Runtime Cutover (SQLite → Postgres), always-working increments

1. Add a DB access layer in the runtime behind the interface `core.py` already uses; implement a Postgres backend (psycopg) reading the existing `DATABASE_URL`.
2. Add `migrations/` (numbered `.sql`) + a small idempotent runner. Port the existing schema + new identity/ledger tables, with constraints + RLS.
3. Seed: create one admin `user`; set `owner_user_id` on `filesystemsmoke*` fixtures.
4. Externalize the per-business filesystem to **Supabase Storage** (sync-down → run → sync-up); local disk = scratch.
5. **Flip** the runtime to Postgres; run full E2E through the real shell; verify identical behavior; then **delete the SQLite path** (no long-term dual backend).
6. **No-fleet proof:** a second runtime with an empty local disk resumes a business purely from Postgres + Storage.
7. Demote the VPS to one stateless worker; once the no-fleet proof passes, the VPS is disposable (scale to N / serverless).

## Security

API keys hashed (SHA-256) + prefix, constant-time compare, one-active-per-user, atomic rotation. Provider creds shared + encrypted (`APP_ENCRYPTION_KEY`), never sent downstream. Generated apps get only a project gateway key. Service-role key lives only in the trusted runtime; RLS on all user-scoped tables. Auth0 token validated server-side (verify issuer/audience/signature), keyed on the stable `sub`; **`ARGON_LOCAL_AUTH_BYPASS` must be off in production**. Stripe webhooks signature-verified + idempotent on event id. Secrets never logged.

## Test & Robustness Strategy (every part has a gate)

- **Real Postgres in tests** (ephemeral/Docker PG), never mocks — billing correctness must hit the real engine.
- **Ledger concurrency/property tests:** parallel reserves/settles never oversell; balances reconcile; idempotency prevents double-charge; reserved never negative.
- **API tests:** opaque-key auth (valid/revoked/rotated), one-active-key invariant, per-user rate limit, no internal leakage, cross-tenant isolation (user A ≠ user B).
- **Idempotency tests:** replayed top-up/checkout/job-submit/webhook → one effect.
- **Failure injection:** provider error → reserve refunded + `blocked`; worker crash mid-job → safe retry; partial artifact → `blocked`, no fake deploy URL.
- **No-fleet stateless-resume proof** (second empty-disk runtime).
- **E2E through the real shell** (`/create`, `/status`, `/pulse`, `/files`, `/read`, `/cron tick`), test businesses in test mode.

## Providers & Infra

- **Now:** Supabase Postgres (source of truth) + Supabase Storage (filesystem/object home — S3-compatible, so **no separate S3/"F3" needed**). Already provisioned.
- **Stripe:** keys present; Connect deferred/optional per the money model.
- **Later:** scale the stateless runtime worker out to N (heavy/build jobs run on the same job contract — **no external job runner**). Optional Temporal/Inngest (durable cron-wake loops) only if durable orchestration is ever actually needed.
- **VPS:** keep now (demoted to a worker), retire after the no-fleet proof. No other new provider required.

## Build Discipline (applies to every phase below)

Two non-negotiable gates run at the **start of every phase**, before any migration, table, tool, or endpoint is written. They exist because the first cut of Phases 1–2 was built greenfield and only checked the live Supabase *afterward* — where `businesses` and `billing_accounts` already exist, populated. Checking what is already present is not optional and not a one-time step; it is the first action of each phase.

### Gate 1 — Inspect what already exists (repo AND backend), then replace / extend / isolate (never parallel)

1. **Backend.** Introspect the real `DATABASE_URL`/`MIGRATION_DATABASE_URL` schema (`information_schema`, `to_regclass`, row counts) for every table/enum/function the phase would create or touch. Assume populated polsia2 tables until proven otherwise.
2. **Repo.** Read the canonical trunk's live model (the SQLite `TakyonStore` in `core.py`) and the existing migrations under `plugins/takyon/db/migrations/`; treat polsia2's migrations as prior art. Know what the runtime reads *today* vs. what is test-only.
3. **Decide, per overlap, exactly one — and write it down before any DDL:**
   - **Extend** the existing table/feature in place.
   - **Replace** a *disposable* polsia2 table deliberately (a migration that drops/renames it) — only after proving it is dead and unreferenced.
   - **Isolate** new tables in a dedicated `takyon` schema so names cannot collide with `public`.
   A new `public.<name>` that shadows a populated polsia2 `public.<name>` is none of these — it is the forbidden third parallel system.
4. **State the finding in the phase**: what exists, what it overlaps, and the chosen replace/extend/isolate decision. Never apply DDL to the shared, populated database without explicit operator confirmation.

### Gate 2 — Surface every new credential / provider need; never bandaid around it

If a phase needs a new secret, API key, service account, OAuth app, or paid provider to work for real, **STOP and tell the operator the exact need** — do not fake it, stub it silently, hardcode a placeholder, or otherwise bandaid around the missing credential to make something look done. For each such need, record **in this file**: the exact env var name(s), the provider/account required, the side effect it enables, the **test-mode** behavior (guarded/stubbed *with a receipt*), and the **live-mode** gate. If the capability works locally with no new credential, say so explicitly. A missing-credential path is `blocked` with a reason (invariant #8) — never a silent fallback or a fake "completed."

**Outstanding new-credential needs:**
- Phases 0–2: none (pure Postgres on the already-provisioned `DATABASE_URL`; Stripe keys already present, Connect deferred/optional).
- **Phase 3 — `STRIPE_BILLING_WEBHOOK_SECRET` (NEW, operator must provide).** Confirmed by the 2026-05-30 decision to use a SEPARATE control-plane webhook endpoint (not the shared product dispatcher) for flow-A topups. **Provisioning step (operator):** in the Stripe dashboard add a webhook endpoint pointing at `https://<control-api-host>/v1/billing/webhook`, subscribe it to `checkout.session.completed`, then copy that endpoint's signing secret (`whsec_…`) into `secrets/.env` as `STRIPE_BILLING_WEBHOOK_SECRET`. **Side effect it enables:** verified crediting of user→platform topups into the billing ledger (`billing.topup`). **Test mode:** tests inject a known secret + locally-signed payload (no real Stripe). **Live gate (invariant #8):** absent → `/v1/billing/webhook` returns `blocked`, never a fake credit. (The checkout-session *creation* needs only the already-present `STRIPE_SECRET_KEY`.)
- **Phase 4 — no new credential.** The execution-policy engine reads existing flow-A balances plus the new per-business `app_execution_policies` table; it moves no money and calls no provider. One OPTIONAL non-secret tuning knob, `TAKYON_EXECUTION_EXPENSIVE_THRESHOLD_CENTS` (default 100 = $1.00), sets the estimate above which a branch counts as "expensive"; a misconfigured value clamps to ≥ 0 (mirrors `STRIPE_CONNECT_APPLICATION_FEE_BPS`'s clamp). Safe default, no operator action.
- **Phase 5 — no NEW external credential.** Verified (2026-05-30) every secret the ported product runtime touches is already present in `secrets/.env`: `STRIPE_SECRET_KEY` (outbound checkout-session creation), `STRIPE_WEBHOOK_SECRET` (product webhook signature — the sub-user/product endpoint, kept DISTINCT from Phase 3's control-plane `STRIPE_BILLING_WEBHOOK_SECRET`), and the platform Anthropic key (`ANTHROPIC_API_KEY`/`ANTHROPIC_TOKEN`, or the CLI auth `get_anthropic_key()`) that `/generate` already uses. Owner→custody accrual (flow B) uses the EXISTING custody ledger + `STRIPE_CONNECT_APPLICATION_FEE_BPS` (present, default 2000 = 20%); the **Connect payout itself stays deferred** (accrual works with no payout credential — invariant: custody accrues from day one). The net-new **project gateway key** is an INTERNALLY-minted per-business credential (hashed like `user_api_keys`, never an operator-provided provider key); it fronts the platform Anthropic key so the generated app never holds it — minting a hash needs no external account. So: no operator action for Phase 5. Live SUB-USER payout to a business owner (real money leaving the platform) is a LATER Connect step and will surface its own credential need then.
- **Phase 7 — Supabase Storage access keys (✅ PROVISIONED + LIVE-VERIFIED 2026-05-31; was: NEW, operator must provide). Recommended S3-compatible quad: `SUPABASE_S3_ENDPOINT` + `SUPABASE_S3_REGION` + `SUPABASE_S3_ACCESS_KEY_ID` + `SUPABASE_S3_SECRET_ACCESS_KEY` (+ our own `TAKYON_STORAGE_BUCKET`); OR the REST alternative `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` (+ `TAKYON_STORAGE_BUCKET`).** This CORRECTS the optimistic Providers note ("Supabase Storage … already provisioned, no separate S3/'F3' needed"): the Supabase *project* is provisioned (we hold `DATABASE_URL`/`MIGRATION_DATABASE_URL`), so no separate object-store *vendor* is required — but `DATABASE_URL` is a Postgres **SQL** connection and **cannot** read/write Storage blob bytes. Live sync-down/sync-up to Supabase Storage needs the access credentials above, which are NOT in `secrets/.env` today. **Provisioning step (operator):** in the Supabase dashboard create a private bucket (e.g. `business-workspaces`) and, under Storage → S3 Access Keys, generate an access key id + secret; copy the endpoint (`https://<ref>.storage.supabase.co/storage/v1/s3`), region, key id, and secret into `secrets/.env` under the four `SUPABASE_S3_*` names, and the bucket name into `TAKYON_STORAGE_BUCKET`. **✅ Done 2026-05-31 (operator signed in to Supabase; agent provisioned in-browser):** created private bucket `business-workspaces` on project four-manifold-prod (ref `ddftvmjpfghfrdxhavvp`, region `us-east-2`) + S3 access key `takyon-business-workspaces`; the four `SUPABASE_S3_*` + `TAKYON_STORAGE_BUCKET` are now in `secrets/.env`, with the selector `TAKYON_STORAGE_BACKEND` deliberately kept COMMENTED (inert; default `local`) until the operator-gated cutover. **Live-verified** end-to-end through the real `storage.py` S3 backend — put→get (sha256 integrity)→list (digest round-trip)→delete on a throwaway key, bucket empty afterward — which closes the prior "wired but unverified against live Supabase" caveat. **Open config note (not yet actioned):** the bucket inherits Supabase's global **50 MB** upload limit while the client cap `MAX_OBJECT_BYTES` is **256 MiB**; raise the global Storage upload limit to ≥256 MiB before live sync of large media, else objects between 50 MB and 256 MiB are rejected server-side. **Side effect it enables:** the externalized per-business filesystem (the host stops being stateful) and the no-fleet stateless-resume. **Dependency:** the live S3 backend lazy-imports `boto3` only when those creds are present; the credential-free path and the whole test suite never import it. **Test mode / local dev — no new credential:** the storage seam ships a real local-filesystem backend (the legitimate "local disk = scratch" + CI tier behind the SAME `StorageBackend` seam, NOT a fake/second path); the no-fleet proof runs fully offline against it (host A syncs up → host B on an empty scratch dir syncs down → business resumes byte-identical). **Live gate (invariant #8):** with the live backend selected and creds (or `boto3`) absent, a sync is `blocked` with a reason — never a silent fallback or a fake "synced." **No DB migration / no new table:** the bucket is the source of truth for file bytes + listing; Postgres stays the source of truth for business/jobs/ledger/schedule state.
- Later phases append here as needs are surfaced.

**Phase 3 gate finding (2026-05-30).** *Gate 2 (credentials):* `secrets/.env` already holds `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PUBLISHABLE_KEY`, `STRIPE_PRICE_PLATFORM_MONTHLY`, `STRIPE_CONNECT_APPLICATION_FEE_BPS`. Flow-A **topups are ad-hoc amounts** (no fixed price id), so the topup checkout itself needs **no new credential** — *unless* it uses a SEPARATE Stripe webhook endpoint, which Stripe signs with a per-endpoint secret (a NEW `STRIPE_*_WEBHOOK_SECRET`). *Gate 1 (redundancy/conflict):* a complete Stripe checkout+webhook already exists in the SQLite trunk — `business_create_app_checkout` + `handle_business_record_stripe_webhook` (core.py:6808/6957), routed at `/api/webhooks/stripe` (app_api.py), deduped via `webhook_events`, written to `app_revenue_events`. **That is the PRODUCT / sub-user side (flow B-ish): a business's customers paying the business.** Phase 3 topup is **flow A (user→platform)** — genuinely new; the ledger primitive `billing.topup(conn,user_id,amount_cents,idempotency_key)` already exists (billing.py:138, idempotent on the Stripe event id). The control-plane topup checkout-session creation (needs only `STRIPE_SECRET_KEY`) and a Postgres-backed per-user rate limiter (no credential, must be PG-backed since workers are stateless) are buildable now. The open decision is the **webhook topology** (shared single endpoint dispatching by metadata = no new secret, but couples to/replaces the SQLite product webhook now vs. at Phase 5; OR a separate control-plane endpoint = clean isolation but a NEW per-endpoint webhook secret). Resolve before building the topup *crediting* path (a checkout with no crediting webhook = money taken, balance unchanged = forbidden fake-`completed`, invariant #8). **RESOLVED + BUILT (2026-05-30):** chose the **separate control-plane endpoint** (`POST /v1/billing/webhook`, its own secret `STRIPE_BILLING_WEBHOOK_SECRET` — see the Gate 2 entry above) for clean flow-A isolation; checkout creation, the crediting webhook, and the PG fixed-window rate limiter are all built and locally verified (see IMPLEMENTATION_LOG "Phase 3" increment). Absent secret → 503 `blocked`, never a fake credit. Still unmounted into the live app; `STRIPE_BILLING_WEBHOOK_SECRET` still operator-pending for live crediting.

**Phase 4 gate finding (2026-05-30).** *Gate 1 (redundancy/conflict):* the SQLite trunk already has a per-business budget — `app_budgets` (core.py:3026, `hard_limit_microusd`, monthly period) + `app_plan_policies` (core.py:3036), enforced in the product `/generate` path (app_api.py:379) and accounted via `handle_business_record_app_usage`. **That is the PRODUCT / sub-user budget: how much a business spends serving ITS customers (microUSD).** Phase 4's `app_execution_policies` is a DIFFERENT concern: the per-business ROUTING knobs (preferred model tier, inline runtime/output ceilings, worker-escalation + expensive-branch toggles, retry depth) the engine reads to route the CEO's OWN compute against the USER's flow-A budget (cents), plus an OPTIONAL monthly sub-cap (`monthly_app_budget_cents`) that is a guardrail, NOT a second wallet (the real balance is always the flow-A ledger). So this is **net-new, ADD-on-top, not a replacement** of `app_budgets` (which is ported as-is in Phase 5). Decision: **ISOLATE** — a new `app_execution_policies` table + a new pure leaf `policy.py`, distinct from both the ledgers and the SQLite product budget; no existing surface absorbs it without conflating the two budgets. The engine is **ADVISORY** — `billing.reserve` (FOR UPDATE) stays the atomic money gate, so a decision moves no money (test-proven). *Gate 2 (credentials):* none — see the Phase 4 Gate 2 entry above (one optional non-secret knob). **BUILT + verified (2026-05-30):** migration `0004_execution_policies.sql` (same fail-loud REPLACE guard pattern as 0001–0003, guards on `preferred_model_tier`) and `policy.py` (`get`/`upsert_execution_policy` + `decide_execution` → inline / cheaper / job / blocked); **19** PG tests green — all four outcomes, escalation + expensive-branch gating, and the per-business monthly sub-cap netted via the reservation_key set (billing's settle/refund carry `business_slug=NULL`, so a direct business_slug filter would miss refunds), plus the advisory no-write invariant. Unmounted; the live AI-gateway call site (resolve business → policy → reserve → settle) is Phase 5. See IMPLEMENTATION_LOG "Phase 4" increment.

**Phase 5 gate finding (2026-05-30).** *Gate 1 (repo, verified against source):* the entire sub-user/product runtime lives in the SQLite trunk and is **owner-agnostic** today. Eleven tables (core.py:3026-3243), all keyed `business_slug → businesses(slug)`: `app_users` (UNIQUE(business_slug,email) — the sub-user identity), `app_magic_links` (token_hash UNIQUE, 15-min), `app_sessions` (token_hash UNIQUE, 30-day cookie `takyon_app_session`), `app_entitlements`, `app_checkout_intents`, `app_checkout_sessions`, `app_revenue_events` (UNIQUE(business_slug,provider_event_id,stripe_object_id)), `app_usage_events` (microUSD metered AI), `app_budgets` (PK business_slug, `hard_limit_microusd` flat cap, NOT a balance), `app_plan_policies` (UNIQUE(business_slug,plan_key)), `webhook_events` (UNIQUE(provider,provider_event_id), global dedup); plus `app_surface_contracts` (product UI contract, not sub-user state). Auth is magic-link only, SHA-256-hashed opaque tokens. HTTP surface in `app_api.py` (`/api/takyon/apps/<business>/…`, server default 127.0.0.1:8787): verify/session/account, checkout, usage, `/generate` (session-required, budget-gated), `/api/webhooks/stripe`. **Three confirmed gaps the acceptance demands:** (1) the legacy `businesses` row has **no owner_user_id** (core.py:2919-2930), so revenue is keyed only to a business — `grep` finds **zero** custody/accrual/payout/Connect/app-fee references in core.py or app_api.py: **no owner accrual exists**; (2) the budget is a **two-path** gate (estimate pre-check in app_api.py:379 + authoritative actuals re-sum at core.py:5362) on a flat microUSD cap, no running balance; (3) `/generate` calls Anthropic with the **platform's shared key** (`_anthropic_key()` app_api.py:66-72) — **no per-business gateway-key boundary**. *Decision:* **PORT** the product tables to Postgres as the successor authority (new guarded migrations, same fail-loud REPLACE-guard pattern as 0001-0004, each guarding on a distinguishing takyon-shape column; introspect live `public` for any same-named polsia2 table BEFORE DDL). This is not the forbidden third parallel system because the SQLite product path is explicitly on death row (Phase 8 = kill SQLite); the Postgres port is its replacement, and Phases 5-7's necessary transitional dual-read must not be allowed to settle into permanent coexistence. **ADD** net-new on top: (a) owner→custody accrual (flow B) — on a paid `app_revenue_events`, resolve `business_slug → businesses.owner_user_id` (the linkage Postgres 0001 added but SQLite lacks) and write a custody entry minus the app fee (`STRIPE_CONNECT_APPLICATION_FEE_BPS`), so "sub-user payment shows in owner custody"; (b) the project gateway-key boundary so "generated app never holds provider key". **COLLAPSE** the two-path budget gate to one authoritative reserve-then-settle. **DROP** the dead `app_plan_policies` fields. *(Correction, 2026-05-30, verified at Phase 5b build time):* the dead set is **only** `stripe_payment_link_id` / `stripe_payment_link_url` (written at core.py:5203/5217-5218/5237-5238, read nowhere). The earlier finding also listed `included_action_quota` and `allow_overage` as dead — that was **wrong**: both are read (rendered into the `product/plans.md` mirror at core.py:3884-3885 and feed `_plan_validation_warnings` at core.py:1995), so they are PORTED, not dropped. Only the two payment-link columns are cruft. *Gate 2 (credentials):* **no new external credential** — see the Phase 5 Gate 2 entry above (all Stripe + Anthropic keys present; Connect payout deferred; the gateway key is internally minted). **Increment (a) BUILT + verified** (sub-user identity/auth/session — migration 0005 + `app_identity.py` + 19 PG tests; see IMPLEMENTATION_LOG "Phase 5a"). Increment (b) — entitlements + plan-policy catalog — in progress.

**Phase 7 gate finding (2026-05-30).** *Gate 1 (repo, verified against source):* the per-business filesystem today is **local disk only** — `TakyonStore.root` (`$TAKYON_HOME`) `/ "businesses" / <slug>` over four canonical roots (`product`, `distribution`, `research`, `metrics` = `TAKYON_BUSINESS_ROOTS`, core.py:58), seeded at `business.upsert` (core.py:4940-4946). It is anchored on the legacy SQLite store and read/written at **~40+ direct call sites** in core.py via `_business_root` / `_resolve_business_file` (path containment) / `_atomic_write_text` / `_append_jsonl`; canonical-relpath aliasing (`brain/→research/`, `app/→product/`, `conversations/→metrics/conversations/`, `receipts/→metrics/receipts/`, `sales/→distribution/`, …) lives in `_canonical_business_relpath` (core.py:2038). A grep for any Storage/S3/sync code (`supabase storage`, `boto3`, `s3`, `put_object`, `sync_down/up`) finds **none** — the only `storage` hits are doc comments and `tools/tool_result_storage.py` (`/tmp/takyon-results` tool-output cache, unrelated). PG migrations 0001–0010 contain **no `files`/storage table** — the Postgres runtime has no filesystem story yet. *Decision:* **ADD a new pure-leaf `plugins/takyon/storage.py`** (mirrors the jobs/wakes leaf shape + the AI gateway's `get_provider_caller` seam): a `StorageBackend` protocol with `get_storage_backend()` selecting a credential-free **local-filesystem** backend by default (CI + the literal "local disk = scratch" tier) or a **Supabase Storage (S3-compatible)** backend when `SUPABASE_S3_*` creds are present; `sync_down(slug, dest)` / `sync_up(slug, src)` are **content-digest incremental** (unchanged files skipped) and reuse the same path-containment discipline (reject `..`, absolute paths, root escapes). This is **REPLACE-on-top, not a third parallel system**: it externalizes the SAME four-root taxonomy that core.py owns; the SQLite local-disk path stays until Phase 8 (the necessary transitional dual-write must not settle into permanent coexistence). **No DB migration / no new table** — the **bucket is the source of truth** for file bytes + their listing; Postgres remains the source of truth for business/jobs/ledger/schedule state (a PG `business_files` manifest was considered and rejected: it duplicates Storage LIST and introduces a bucket↔table two-write drift hazard). *Gate 2 (credentials):* the LIVE object-store backend needs NEW Supabase Storage access keys (see the Phase 7 Gate 2 entry above) — `DATABASE_URL` alone cannot reach blob bytes — so the plan's "no new credential" note for Storage is corrected; absent creds (or `boto3`) in live mode → `blocked` with a reason (invariant #8), never a fake "synced." The mechanism (seam + local backend + the no-fleet proof) is fully buildable + testable now with **no new credential**; the live wire-up (selecting the S3 backend, and wrapping the worker's per-job run in sync-down→run→sync-up) is left as a tested, inert seam until the operator-gated cutover, exactly as Phase 6 left the worker loop unmounted.

**Phase 8 gate finding (2026-05-31).** *Gate 1 (repo AND live backend, verified against source):* the SQLite authority is `TakyonStore` (`core.py:2901`); its `_init_db` `executescript` (core.py:2919-3246) is the authoritative **22-table** operator schema. **12 already have a Postgres home** — `businesses` (0001) and the eleven `app_*` product tables (`app_users`/`app_magic_links`/`app_sessions` 0005, `app_entitlements` 0006, `app_usage_events`/`app_budgets`/`app_plan_policies` 0007, `app_checkout_intents`/`app_checkout_sessions`/`app_revenue_events`/`webhook_events` 0008). **10 are SQLite-only** with no PG home: `workspaces`, `jobs` (operator), `agent_runs`, `ledger_entries`, `control_states`, `events`, `conversation_threads`, `conversation_messages`, `idempotency_keys`, `app_surface_contracts`. The connection surface is a clean single seam: one opener `_connect()` (core.py:2909), **16** `with self._connect()/store._connect() as conn:` entry points, **26** `conn`-taking helpers, all `conn.execute(<sql with ? params>).fetchone()/.fetchall()` + `_row_to_dict` (core.py:3329); no AUTOINCREMENT, no SQL date/JSON functions (timestamps are Python ISO strings via `_now()`; JSON is `json.dumps`'d TEXT), only **2** `INSERT OR IGNORE` (core.py:6923/6978) and 14 already-portable `ON CONFLICT`. *Live introspection (read-only, authorized):* live `public` holds **125** base tables. Of the port names five collide: `businesses` + `jobs` are **already the takyon shape** (the 0001/0010 apply, both 0 rows) — leave as-is; **`agent_runs` (70 rows), `events` (1274 rows), `idempotency_keys` (0 rows) are polsia2 shape** and must be retired before porting (polsia2 lacks `scope`/`event_type`/`operation_hash` respectively — the distinguishing guard columns). The other seven names are absent → clean install. **The `events` collision (1274 polsia2 rows) was invisible to the repo-only view; only the live read surfaced it** — the reason Gate 1 mandates inspecting the backend. *Per-table decision:* `businesses` → **ENRICH** (ALTER ADD goal/status/work_focus/budget_json/metadata_json/updated_at + work_focus CHECK + the two `businesses_*_idx`); same entity as 0001's slim row, stays one table; `owner_user_id` stays NOT NULL (the store seam resolves/seeds an owner — it has none today); created_at/updated_at stay `timestamptz` (0001 consumers expect it) with the seam coercing datetime↔ISO. `workspaces`/`control_states`/`events`/`conversation_threads`/`conversation_messages`/`app_surface_contracts` → **PORT** faithfully (new tables, identical columns; events + conversation indexes recreated). `agent_runs`/`idempotency_keys` → **PORT** + retire the polsia2 collision. `jobs` (operator) → **ISOLATE** as `business_work_requests`: the operator `jobs` is a *work-request record* (`business_enqueue_job`, INSERT core.py:5518; only counted/listed/GC'd — core.py never drains it), categorically different from the 0010 worker-plane `jobs` *execution queue* (uuid, idempotency_key, reserved_billing_entry_id, FOR UPDATE SKIP LOCKED); folding them would pollute the queue, isolating preserves both exact semantics (consolidation onto the worker plane is a later *behavioral* step, not this storage port). `ledger_entries` → **PORT** faithfully (amount REAL→double precision); it is the operator's *per-business internal allocate/spend budget ledger* (balance SUM core.py:5493), distinct from flow-A `billing_entries`, flow-B `custody_entries` (the real money), and `app_budgets` (sub-user microUSD cap) — no PG table covers it. *Types:* the 10 new operator tables store created_at/updated_at/received_at as **`text`** (1:1 with the SQLite ISO strings; the only reader is the ported operator store, which compares/returns ISO strings) and `*_json` columns as **`text`** (round-trips `json.dumps`/`_row_to_dict`'s `json.loads`). This differs from 0001-0010's `timestamptz`/`jsonb` deliberately — those tables are read by psycopg leaf code written for datetimes/dicts, the operator tables by string-speaking core.py; different access code for different tables, the smallest faithful port, not a parallel system. *Known next risk (sequenced, NOT this increment):* the 11 PG `app_*` tables (0005-0008) were **refined** vs core.py's SQLite DDL (Phase-5 finding dropped `stripe_payment_link_*`, collapsed the two-path budget, added owner accrual), so the operator store's `app_*` SQL will not match the PG shapes; resolution belongs to the store-seam increment (reconcile core.py's `app_*` SQL to the PG shapes, OR stop having the operator store own `app_*` and defer to the psycopg leaf modules). The migration increment is additive and does not touch this. *Increment sequencing (big-bang port, loop-until-green):* (1) **this finding + `0011_operator_runtime.sql` (10 operator tables + businesses enrich, each fail-loud REPLACE-guarded) + extend `retire_polsia2_public.sql` for the agent_runs/events/idempotency_keys collisions + local PG test** ← now; (2) the **Postgres-backed store seam** in `TakyonStore` (`_connect`→psycopg, `?`→`%s`, `dict_row`, datetime coercion, the 2 `INSERT OR IGNORE`→`ON CONFLICT`, app_* reconciliation); (3) **owner wiring** (seed platform user, Auth0 JIT — task #6); (4) **serving flip** (`./takyon` shell + dashboard → PG) + pg_cron dispatch + `TAKYON_STORAGE_BACKEND=supabase_s3`; (5) **full E2E through the real shell, loop until green**; (6) **delete the SQLite path**; then the no-fleet proof. *Gate 2 (credentials/providers):* **NONE new.** One Supabase **config** toggle: enable the in-DB `pg_cron` extension so `dispatch_due_wakes()` (0010) runs on an interval without a local ticker; the equivalent CRON_SECRET-bearer endpoint (CRON_SECRET already provisioned) runs the same SQL if pg_cron is not enabled, so even this is optional — no new account, key, or paid service. (Standing operator reminders unchanged: `STRIPE_BILLING_WEBHOOK_SECRET` still pending for live flow-A crediting; the 50 MB→≥256 MiB Storage upload-limit decision still open.)

**Phase 8 store-seam finding (2026-05-31, step 2 of the Phase-8 sequence).** *Gate 1 (repo, verified against source THIS increment):* the store's connection surface is even cleaner than the gate finding noted — **15** `with (self|store)._connect() as conn:` entry points, **0** positional row accesses (all 319 reads are by column name or via `_row_to_dict`'s `dict(row)`, so psycopg `dict_row` is a drop-in for `sqlite3.Row`), every `?` that reaches `execute()` is a genuine placeholder (including the dynamic `",".join("?" …)` IN-lists), **0** literal `%` in any store SQL string (the only `%` is the LIKE wildcard, which rides in the bound *parameter* — psycopg leaves params untouched), and the entire sqlite-specific surface is just `row_factory` + one `executescript` + `_init_db`/`_migrate_db` (no executemany/cursor()/commit()/rollback()/create_function). *Decision — connection mechanism:* a **thin translating connection wrapper** returned by `_connect` on the PG backend, NOT the literal per-site `?`→`%s` the gate finding sketched. The wrapper sets `row_factory=dict_row`, and its `execute()` escapes any literal `%`→`%%` then translates `?`→`%s` before delegating to `psycopg`; all ~154 call sites stay byte-identical. This is more robust AND more parsimonious than 154 hand-edits: one well-tested translation point (no missed conversion), zero interleaving with the operator's uncommitted meta-ad hunks in the same file, and the operator's future store SQL works unchanged. *Decision — transaction model:* the store wrapper connects **autocommit=False**, so `with self._connect() as conn:` is exactly one atomic transaction (commit at block exit, rollback on exception) — precisely the sqlite3 `with conn:` semantics `_apply_operation`'s grouped writes depend on; psycopg3's native `with conn:` also closes the fresh-per-block connection (no leak). This SUPERSEDES the earlier "needs explicit `conn.transaction()`" note, which described the leaves' autocommit=True per-request pattern, not the store. *Decision — backend switch:* `_connect` branches on backend (default **SQLite** — the serving flip is step 4); the PG branch reuses `runtime_app.resolve_database_url`, opens `psycopg.connect(url, row_factory=dict_row)` (autocommit=False), wraps it, and SKIPS `_init_db`/`_migrate_db` (0001-0011 own the schema); `self.root.mkdir` stays for the business filesystem. *Decision — app_\* (resolves the gate finding's open sub-decision to **Option B = defer to the psycopg leaf modules**), now confirmed against the migration shapes:* the usage path is ARCHITECTURALLY forced — 0007 `app_usage_events.reservation_key NOT NULL` + the reserve→settle gate; the store's flat re-sum-actuals write is the exact invariant-#8 money-truth violation 0007 removed — and the remaining app_\* writes diverge only mechanically (metadata_json→`metadata` jsonb, dropped `stripe_payment_link_*`, allow_overage int→bool), but `app_identity.py`/`app_entitlements.py`/`app_payments.py`/`app_usage.py` already implement every one correctly against the PG shapes, so reconciling the store's SQL (option A) would perpetuate exactly the duplicate logic the build discipline forbids. The leaves are the Phase-5-declared successor authority. *Staging (each independently PG-verifiable; the SQLite default keeps the whole suite green throughout):* **(2a, this increment)** the wrapper + `_connect` backend switch — the operator tables (`businesses` + the 10 ported by 0011) round-trip on PG, verified by PG tests over the operator operations (business/workspace/job/agent_run/ledger/control_state/event/conversation/idempotency/surface-contract); app_\* writes are not yet PG-valid but are not exercised by these tests, and the default SQLite path is untouched. **(2b, next)** delegate the store's app_\* reads/writes (in `_apply_operation`, `read`, `calculate_pulse`, and the module handlers) to the leaves; verify the app_\* operator paths on PG. *Gate 2:* none new — a pure code seam on the existing `DATABASE_URL`.

**Phase 8 store-seam step 2b finding (2026-05-31, step 2b of the Phase-8 sequence).** *Gate 1 (repo, verified against source THIS increment):* this slice carries the two divergences the 2a seam deliberately left unhandled — the operator `jobs` table-name and the `app_*` shapes — in two independently PG-verifiable stages. **Stage A — operator-jobs storage retarget.** The store names the physical table `jobs` at **8** sites: the enqueue INSERT (core.py:5622), the pulse window read (:4181) and queued count (:4330), the list read (:4820), the GC select+delete (:5906/:5922), and the business-delete count/delete loops (the `by_business` list entry :4577 and the two `("jobs","ledger_entries","events")` tuples :4599/:4629). On the PG backend that bare literal would hit the **0010 worker-plane queue** (uuid/jsonb/`FOR UPDATE SKIP LOCKED`) — the wrong table. 0011 already built `business_work_requests` as the exact 1:1 column port of the SQLite operator `jobs` (`id/scope/business_slug/kind/status/payload_json/created_at/updated_at`, all `text`) — the storage home the P8.1 ISOLATE decision named. So Stage A interpolates a `_work_requests_table()` helper (returns `business_work_requests` on PG, `jobs` on SQLite) into those 8 sites. This is a **PURE STORAGE retarget** — identical columns, identical semantics, the store still only inserts/counts/lists/GCs and never drains it — explicitly **NOT** the deferred worker-plane consolidation (a later *behavioral* step per the P8.1 finding). Where the table name doubles as a receipt-dict key (the count/delete/GC results), the physical name flows through to the receipt on PG (truthful: it IS the table touched); the SQLite default stays byte-identical so the existing suite is untouched. **Stage B — app_\* delegation to the Phase-5 leaves** (realizes the 2a finding's Option-B decision against the leaf source). Verified this increment: every leaf (`app_identity`/`app_entitlements`/`app_usage`/`app_payments`) (i) takes a psycopg `conn` as its first arg, (ii) reads rows **positionally** (`row[0]`/`row[1]` — e.g. app_usage `_budget_from_row` at :141/:214, NOT by name), (iii) embeds **literal `%s`** placeholders, and (iv) opens its **own** `with conn.transaction():` per mutating op. Those four facts fix the delegation shape exactly: the store must hand the leaf the **raw** psycopg conn (`_PGConn._pg`), never the `_PGConn` wrapper — the wrapper's `?`→`%s` translate (which also escapes `%`→`%%`) would corrupt the leaf's literal `%s` into `%%s`; it must swap that conn's `row_factory` off `dict_row` to the psycopg default tuple rows for the leaf call (else the positional `row[0]` raises) and restore `dict_row` after; and it must reuse the **same** connection so the leaf's `with conn.transaction():` nests as a **SAVEPOINT** inside the store's already-open outer transaction (the store connects `autocommit=False`), keeping the leaf writes atomic with the store's `idempotency_keys` row. A `@contextmanager _leaf_conn(self, conn)` helper does the unwrap+swap+restore. *Store-op → leaf-fn mapping (with the shape divergences each absorbs):* `app.budget.set` → `app_usage.set_app_budget` (+`ensure_app_budget`); `app.plan.upsert` → `app_entitlements.upsert_plan_policy` (the store's INSERT drops the dead `stripe_payment_link_id/url` and passes `allow_overage` as a real bool, not the SQLite int); `app.customer.upsert` → `app_identity.upsert_app_user`; `app.entitlement.upsert` → `app_entitlements.grant_entitlement` (both enforce anti-fake-billing — the store's `TakyonError` becomes the leaf's `FakeBillingRejected`); `app.usage.record` → `app_usage.record_completed_usage` (the leaf's atomic record-with-cap **replaces** the store's flat re-sum-of-actuals + raw INSERT, which is precisely the invariant-#8 money-truth violation 0007's reserve→settle model removed). `app.surface.upsert` / `app.surface.publish_result` get **NO delegation** — `app_surface_contracts` is 0011-owned and already round-trips through the 2a seam. *Reads:* the `app_*` delete/count loops (core.py:4594-4605/4626-4634) are **shape-agnostic** (every PG `app_*` table carries `business_slug`/`scope`), so they work through the 2a seam unchanged; only the diverging reads delegate — PG `app_usage_events.created_at` is `timestamptz` (the store compares ISO-string params) and PG `app_users.metadata` is `jsonb` named `metadata` (the store expects text `metadata_json`) — routed through the leaf read fns (`get_usage_summary`/`list_usage_events`/`get_app_budget`/`list_entitlements`/`list_plan_policies`/`list_revenue_events`). *Staging:* A then B, each verified by new PG tests over the affected operator/app_\* operations; the SQLite default keeps the whole suite green throughout (the helper and `_leaf_conn` branch only on `_db_backend()=="postgres"`). *Gate 2:* none new — a pure code seam on the existing `DATABASE_URL`.

**Phase 8 owner-wiring finding (2026-05-31, step 3 of the Phase-8 sequence; task #6).** *Gate 1 (repo, verified against source THIS increment):* the store's `business.upsert` INSERT (`core.py:5111`) supplies **no** `owner_user_id`, but PG `businesses.owner_user_id` is `NOT NULL references users(id)` (0001 spine; 0011 line 46 already declares "the store seam resolves/seeds an owner") — so on the PG backend a new-business INSERT fails the not-null. The UPDATE path (`core.py:5106`, existing-business branch) never touches `owner_user_id`, so it already runs unchanged on PG. The store has **no Auth0/login context** (it serves the local CEO/shell), so it cannot learn an owner per-request. `control_plane.py` is the canonical owner-provisioning module: `get_or_create_user(conn, auth0_sub, email)` (JIT, idempotent on `auth0_sub`), `provision_user_on_first_login` (one txn: ensure user + mint THE single API key + open billing + custody — so a half-provisioned owner can never be observed; returns the raw key exactly once), `resolve_api_key`. `web_server.py`'s `/auth/callback` (`:846`) already runs full Auth0 dashboard auth and builds a verified `user` dict with `sub`/`email`/`name` (`:898`) — but **never calls the JIT** (task #6, deferred to this cutover). `control_api.py`'s `/v1/businesses` (`:140`) scopes by `where owner_user_id = principal.user_id`, so for the dashboard to show businesses the local CEO created, the shell's `business.upsert` owner and the dashboard login MUST resolve to the **same** `users` row. *Decision — single platform owner, config-keyed:* the local shell/CEO owns every business it creates as ONE platform/operator user, keyed by `auth0_sub` read from **`TAKYON_PLATFORM_OWNER_SUB`** (default sentinel `takyon|platform-owner`, optional `TAKYON_PLATFORM_OWNER_EMAIL`); setting that env to your real Auth0 `sub` unifies the shell-owned businesses with what your dashboard login sees (matches the memory "shared platform accounts" model). *Decision — minting stays OUT of the store (no secret through a commit):* `business.upsert`'s PG branch resolves the owner **read-only** via a new `control_plane.resolve_platform_owner_id(conn)` (a bare `select id from users where auth0_sub=%s`, run over the **raw** psycopg conn lent by the existing `_leaf_conn` so it speaks `%s`+positional like the other leaf calls); unprovisioned → `TakyonError` **blocked with an actionable reason** (invariant #8), never a NULL/fake owner. The one-time raw API key is therefore **never** observable in a commit result, event payload, or file mirror — it is surfaced only by the explicit `control_plane.ensure_platform_owner(conn)` bootstrap (which wraps `provision_user_on_first_login`, returns `(user_id, raw_key)`), called at the serving-flip startup (step 4) and by the dashboard JIT. *Decision — only the INSERT forks:* the PG INSERT adds the `owner_user_id` column+value; the SQLite INSERT stays byte-identical (no such column); the UPDATE path is unchanged on both. The INSERT's `mode` value (`mode or "live"`) is always within `_BUSINESS_MODES={"live","test"}`, which equals 0001's `businesses_mode_chk`. *Decision — Auth0 JIT (#6):* `/auth/callback`, right after `_auth0_authorize_claims`, calls `provision_user_on_first_login(conn, user["sub"], user["email"])` **only when `_db_backend()=="postgres"`** (a guarded no-op in the SQLite era — there is no `users` table to provision into, and the dashboard still logs the operator in via its signed cookie exactly as today), on a per-request `autocommit=True` psycopg conn mirroring `runtime_app`'s `control_conn`; on the rare brand-new `sub` it surfaces the one-time key via a prominent server log (never a cookie). *Staging:* this increment adds the two `control_plane` helpers + the `business.upsert` INSERT fork + the `/auth/callback` JIT, with new PG tests proving `business.upsert` lands a business with a resolvable `owner_user_id` and that an unprovisioned owner blocks with a reason; the SQLite suite stays green (the fork branches only on `_db_backend()=="postgres"`). The startup `ensure_platform_owner` seed call rides with the serving flip (step 4) in the same push. *Gate 2 (credentials/providers):* **ONE new config identifier, NOT a credential** — `TAKYON_PLATFORM_OWNER_SUB` (+ optional `TAKYON_PLATFORM_OWNER_EMAIL`). It is a non-secret string with a working default, so zero-friction local is unaffected; setting it to your real Auth0 `sub` is the optional shell↔dashboard unification. No new account, API key, OAuth app, or paid service. (Standing operator reminders unchanged: `STRIPE_BILLING_WEBHOOK_SECRET` still pending for live flow-A crediting; the Supabase `pg_cron` enable toggle; the 50 MB→≥256 MiB Storage upload-limit decision still open.)

**Phase 8 serving-flip finding (2026-05-31, step 4 of the Phase-8 sequence).** *Gate 1 (repo AND serving topology, verified against source THIS increment):* the flip is fundamentally **env-driven and already-seamed** — `_db_backend()` (core.py:2922) reads `TAKYON_DB_BACKEND` (default `sqlite`, opt-in `postgres`) and `get_storage_backend()` (storage.py:329) reads `TAKYON_STORAGE_BACKEND` (default `local`, opt-in `supabase_s3`); both are explicit, NOT auto-detected from `DATABASE_URL`, so no process flips silently. The store's PG branch (`_connect_postgres`, core.py:3023) already resolves the URL via `runtime_app.resolve_database_url` and skips DDL. So "flip the runtime to Postgres" is **setting two env vars at the serving entrypoints, not new connection code**. *The two serving entrypoints* (verified): the interactive shell `_interactive_shell` (cli.py:1913, reached via `./takyon` → `plugins.takyon.cli:main`), where the CEO creates businesses through `TakyonStore`; and the dashboard server `start_server` (web_server.py:5859, reached via `takyon dashboard` → `cmd_dashboard`, the long-lived VPS process), which constructs `TakyonStore(get_takyon_home())` and serves uvicorn. *The owner gap (the one substantive wire):* on PG, `business.upsert` resolves the platform owner READ-ONLY and **blocks** if unprovisioned (P8.3, invariant #8) — minting deliberately stays out of the commit path — so SOMETHING must seed the owner once at startup. *Decision — one idempotent seed method, reused at both entrypoints:* add `TakyonStore.seed_platform_owner()` (core.py) that no-ops off PG and on PG opens ONE store transaction and delegates to `control_plane.ensure_platform_owner` over the **raw** psycopg conn lent by the existing `_leaf_conn` (the same unwrap the app-leaf delegations use, because control_plane speaks native `%s`+positional). This REUSES the P8.2 connection seam rather than introducing a second connection strategy (parsimony); it is the ONE place a key is minted as a serving side effect, returned once to the caller (`(user_id, raw_key)`), never through a commit/event/file. The shell calls it after `store = TakyonStore()` and prints the one-time key to stderr once; the dashboard calls it in `start_server` before `uvicorn.run` and logs the key once at WARNING (mirrors the P8.3 `_provision_dashboard_user_if_postgres` style). Both wrappers NEVER crash startup on a seed failure — a later `/create` would block with its own actionable reason. *Decision — storage flip is pure env:* `TAKYON_STORAGE_BACKEND=supabase_s3` already activates the live S3 backend (storage.py, Phase-7-built+live-verified); nothing to wire here beyond setting it at cutover (kept COMMENTED/inert in `secrets/.env` until then). *Decision — wake-dispatch home = pg_cron, as a canonical inert seam:* added `db/apply_pg_cron_dispatch.sql` (deliberately OUTSIDE `db/migrations/` so the local runner/conftest never sweep it — pg_cron is Supabase-/superuser-only and absent locally; it raises loudly if applied without pg_cron or without 0010's `dispatch_due_wakes()`). It `cron.schedule`s `select dispatch_due_wakes()` every minute, idempotently (unschedule-by-name then schedule = exactly one job), with a one-statement `cron.unschedule` revert. **Honesty:** `dispatch_due_wakes()` is ENQUEUE-ONLY; the worker DRAIN (running the CEO wake turn) is the Phase-6 worker plane that was deliberately left UNMOUNTED — confirmed THIS increment that `jobs.run_one` (jobs.py:308) is a `handlers`-seam with no caller, so mounting the drain needs a CEO-turn handler registry (it runs the model), which is **out of P8.4 scope** exactly as Phase 6 left it. So P8.4 establishes the dispatch home as a ready, inert seam (a due wake would accumulate as a queued job once applied); it does NOT claim end-to-end wake processing. *Gate 2 (credentials/providers):* **NONE new.** Two env *toggles* (`TAKYON_DB_BACKEND=postgres`, `TAKYON_STORAGE_BACKEND=supabase_s3`) on already-provisioned `DATABASE_URL` + `SUPABASE_S3_*`; one optional Supabase **config** toggle (enable `pg_cron`) for the dispatch script, with the CRON_SECRET-endpoint/ticker alternatives running the identical SQL — no new account, key, OAuth app, or paid service. The non-secret `TAKYON_PLATFORM_OWNER_SUB` identifier (P8.3) keys the owner seed. (Standing operator reminders unchanged: `STRIPE_BILLING_WEBHOOK_SECRET` still pending for live flow-A crediting; the Supabase `pg_cron` enable toggle; the 50 MB→≥256 MiB Storage upload-limit decision still open.)

## Phased Rollout (each phase leaves the system working)

> Every phase begins by running **both Build Discipline gates above** (inspect-what-exists → replace/extend/isolate; surface-credential-needs). A phase is not "started" until its inspection finding and any new-credential needs are written down.

- **Phase 0 — DB layer:** Postgres access layer + migration runner; CI on real Postgres. *Accept:* runtime reads/writes Postgres; migrations idempotent.
- **Phase 1 — Identity spine (STEP 1):** `users`, `user_api_keys` (one-active invariant), `businesses.owner_user_id`, payout fields; resolver `api_key → user → businesses → policy`; seed fixtures; route `GET /v1/me` + `/v1/businesses` through it. *Accept:* a request resolves to exactly one user + their businesses before any provider/worker call; revoked key rejected; second active key refused.
- **Phase 2 — Two ledgers:** billing + custody, reserve-then-settle, allowance-first, exact topup, idempotency, reconciliation. *Accept:* costly action → correct entries; double-charge impossible under concurrency; allowance never shown as dollars; custody accrues without Connect.
- **Phase 3 — Control API:** opaque-key boundary + rate limiting + topup checkout + payout/Connect (deferred). *Accept:* full opaque boundary; per-user rate limit; zero-connection onboarding.
- **Phase 4 — Execution policy engine:** inline vs job vs cheaper vs blocked from allowance/topup/policy/estimate. *Accept:* features degrade gracefully under budget pressure instead of hard-failing.
- **Phase 5 — App runtime API:** sub-user auth/session/account/usage/checkout/generate/jobs; sub-user payments accrue to owner custody minus app fee; project gateway keys. *Accept:* all apps share rails; sub-user payment shows in owner custody; generated app never holds provider key.
- **Phase 6 — Worker plane + scheduled wakes:** jobs queue, idempotent, retries-with-budget, receipts; heavy jobs run on the same job contract drained by the runtime worker (no external job runner); `wake_schedules` + `pg_cron` dispatch enqueues due CEO wakes into the queue (no local ticker). *Accept:* heavy work runs as jobs; partial = blocked/failed; a due `wake_schedule` enqueues exactly one job and the worker drains it; a host outage fires one catch-up wake, not a backlog; **no SQLite/jobs.json/systemd cron path is reintroduced.**
- **Phase 7 — Externalize filesystem + no-fleet proof:** Storage-backed workspaces; second empty-disk runtime resumes; demote/retire VPS. *Accept:* second host resumes from Postgres+Storage; VPS disposable.
- **Phase 8 — Kill SQLite:** delete the SQLite authority path. *Accept:* no SQLite authority remains; all E2E green on Postgres.

**Do first:** Phase 0 + Phase 1 — the Postgres data layer and the identity spine + resolver with one read routed through it. Everything else plugs into that.
